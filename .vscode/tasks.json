{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Library App",
            "type": "shell",
            "command": "mkdir -p build && g++ -std=c++17 src/*.cpp -o build/app && ./build/app",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Builds both Book.cpp and main.cpp, then runs the program."
        }
    ]
}